// ignore_for_file: unused_element, unused_element_parameter
// Removed: this widget was added during an experimental change and later
// reverted. The original invite button remains inline in
// `pantalla_sala_espera.dart`.
import 'package:flutter/widgets.dart';

class _PlaceholderInviteButton extends StatelessWidget {
  const _PlaceholderInviteButton({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
