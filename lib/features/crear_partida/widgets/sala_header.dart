// ignore_for_file: unused_element, unused_element_parameter
// Removed placeholder for SalaHeader — kept minimal to avoid warnings.
import 'package:flutter/widgets.dart';

class _PlaceholderSalaHeader extends StatelessWidget {
  const _PlaceholderSalaHeader({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
