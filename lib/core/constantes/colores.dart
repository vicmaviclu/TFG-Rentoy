import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2E8B57); // más cercano al degradado verde
  static const Color secondary = Color(0xFFFFC107);
  // Fondo general: un verde muy claro para que la 'tarjeta' no se vea blanca
  static const Color background = Color(0xFFEFFAF0);
  // Superficie clara (si hace falta usar blanco para elementos puntuales)
  static const Color surface = Color(0xFFF7FFF9);
  static const Color card = Color(0xFF0F1724);
  static const Color textPrimary = Color(0xFF0B1020);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color accent = Color(0xFF00C853);
  static const Color googleGrey = Color(0xFFF1F3F4);
}
